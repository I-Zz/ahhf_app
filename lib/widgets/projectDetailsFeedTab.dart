import 'package:flutter/material.dart';

class projectDetailsFeedTab extends StatelessWidget {
  const projectDetailsFeedTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Feed'),),
    );
  }
}